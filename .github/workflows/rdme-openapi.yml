# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-08-17T06:02:36.445Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `master`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - master

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi swagger.yaml --key=rdme_xn8s9hf0babbe86a0d741113329e93cdd14ce17b3b1544dcc58f620dff306d00e4eb5e --id=64dcf7dd37a33e1973b3c211
